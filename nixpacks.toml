[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m venv /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "gunicorn 'app:create_app()' --chdir backend --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
